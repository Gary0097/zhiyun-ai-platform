# 2.2.0 交付验收记录

跟踪：#138。基线：master `e021f0a841072671f29df8bd165ab2bb01aabcf9`。
本记录为验收进行中的证据，不代表正式发布或全功能验收通过。

## 已复现的问题与修复

- 重复品牌补丁会匹配已注入脚本注释中的 HTML 结束标签，导致主题进入脚本内部。现在在真正的 head 结束位置插入，重复应用测试检查样式仍位于脚本外。
- 深色系统主题下，白色登录背景与白色标题/安全提示混用。登录页现在完整采用浅色样式，已登录页面保持原主题。
- 两层 `.qwenpaw-app` 同时分栏，低高度窗口裁切。现在仅外层 Spark 容器分栏，登录根节点允许纵向滚动。真实 Hub 在 900×500 和 320×568 下检查，320px 下输入宽度约 229px，无横向溢出。
- Ubuntu Hub 使用相对模块名读取锁文件，Node 报 MODULE_NOT_FOUND。改为绝对路径。
- 全新 Hub 直接绑定 0.0.0.0 被上游拒绝。启动器先只绑定本机，检测到有效管理员后允许团队访问，不放宽上游认证。
- Windows 非管理员能够打开 Hub 控制台，但创建 Local runtime 返回 503。启动入口现在明确处理管理员权限，而不是警告后假装可用。
- Ubuntu uv Python 的 minor-version 符号链接不在 bubblewrap 挂载白名单内，隔离运行时报解释器不存在。安装脚本将自身 venv 的解释器链接规范化为真实路径，不扩大沙箱文件系统权限。
- 安装器 Node 版本检查原来只打印 false 而不返回失败。已修正为真实退出码检查。
- 安装器升级解压保留已有 Hub 配置和工作区文件；路径穿越直接失败。
- 桌面启动器停止实例时存在未定义 `$root`，以及归属检查异常时放行的问题，已改为自身安装路径匹配与失败关闭。
- 单机诊断 `--check` 不再触发旧实例停止；重复安装不再安装已解耦的 Creator 依赖。

## 当前验证证据

| 范围 | 结果 | 限制 |
|---|---|---|
| Windows 单机 2.2.0 安装 | 已完成 | 全新环境后台加载约 252 秒，已将桌面等待窗口扩大到 600 秒；仍需完成整包冷启动验收 |
| 单机原生认证 | 首次注册、登录、token 验证、匿名拦截、第二次注册拒绝通过 | 测试账号仅存在忽略的验收运行时内 |
| Windows Hub 认证 | 管理员/员工登录与管理接口权限通过 | Local runtime 创建需管理员会话，尚待提权后的完整验证 |
| Ubuntu 24.04.4 WSL2 | 已装配真实 Hub 与 bubblewrap，控制台启动通过 | 普通 Docker 不支持所需命名空间，已改为独立 WSL2 实机环境；隔离 runtime 修复复测中 |
| CSS 回归 | 原 fixture 套件及重复注入回归共 58 项通过 | 还需补充内页路由、深浅主题和完整视窗矩阵的实际检查 |
| 安装器代码测试 | 数据/Hub 配置保留、特殊路径、ZIP 越界拒绝、Node 检查、安装子进程错误码与日志捕获通过 | 安装界面已重写并渲染，完整离线包/升级/卸载验收未完成 |
| 真实模型调用 | 待验证 | 当前环境未提供测试模型凭据，已向负责人询问，不使用模拟回复冒充 |

## 运行命令

```text
node scripts/verify-release.mjs
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/test-installer.ps1
python scripts/test-hub-bootstrap.py
node scripts/test-hub-config.mjs
```

`scripts/acceptance/probe-auth.py` 只允许 loopback，需显式允许测试注册，只用于
隔离验收实例。不得针对已有客户实例运行。测试账号状态必须留在 Git 忽略目录。
HTTP 认证探测不等同于真实模型、文件系统隔离或完整 GUI 验收。

## 仍需完成

### 2026-09-10 增量验证

- 已纳入 PR #137 的文档一致性修改及对应断言；当前本地品牌化回归共 61 项通过。
- 安装器专项检查 9 项通过，新增整包路径预检与规范化工作区路径保护：非法后续 ZIP 条目不会先覆盖已安装文件，路径别名不会绕过工作区保护。
- 静默安装在系统集成失败、载荷缺失或离线安装入口缺失时不再报告成功。
- Hub HTTP 探测现在要求启动返回 200 且状态为 running；启动失败不再被退出码 0 掩盖。
- 完整发布门禁通过。以上是源码和专项验证，不代表候选包安装、升级、卸载已验收。
- Ubuntu 验收发行版仍存在；本次进程检查确认旧 Hub 服务已停止，下一次实机测试需明确重新启动。
- 随后已重新启动 Ubuntu Hub：管理员已存在时监听 0.0.0.0:8000；原生认证、员工管理接口拒绝、跨用户 runtime 读取拒绝均通过；runtime 启动返回 200/running，强化后的探测退出码为 0。文件系统隔离、凭据注入和真实模型仍待验收。
- 已从本地提交 a0be365da6 启动 2.2.0 离线候选包构建，内嵌 Windows Python/uv 缓存和 Node，构建尚未完成时不得提供为可用安装包。

- Windows 管理员会话下的 Hub runtime 创建、启动、隔离与凭据注入。
- Ubuntu Hub runtime 成功启动、跨账号隔离及重新启动后团队访问。
- 真实模型对话与配置后的用户流程；不输出密钥或客户数据。
- 图形安装器最终布局/进度/失败重试、卸载体验、干净机离线安装及覆盖升级。
- 2.2.0 候选 EXE/ZIP、SHA256、实际冷启动证据；正式发布仍需评审。
- 文档完整对齐，旧 Issue 按完成/替代/仍有风险分别处理，不能统一标为已完成。

## 旧 Issue 分流（2026-09-10 已更新云端）

| Issue | 建议处理 | 原因 |
|---|---|---|
| #64 | 已以 not_planned 关闭，由 #138 替代 | 原目标固定 2.1.0，已不符合 PRD V7 |
| #77 | 已以 not_planned 关闭旧范围，现行原生认证验收归 #138 | 不能以新架构冒充旧八应用集成验收 |
| #83 | 保留旧 seeder 安全风险，明确当前发行版不包含该插件 | 移除代码不证明旧版本越权漏洞已修复 |
| #91、#93 | 已移出 codex-ready 队列，保持开放 | Studio 已解耦，无主线 PawApp 锁，恢复为可选应用时另行验收 |
| #126 | 保留并对齐混合插件/补丁实现 | 完全升级免疫仍未证实，不能自动关闭 |

## 回退与数据边界

代码在独立任务分支。回退代码不删除 Workspace、Hub control.db、secrets 或客户数据。
运行时可重建；任何升级/卸载必须保留用户工作区。测试数据和 WSL 分发版为本次验收专用，
不作为安装包内容，不把本机验收日志或凭据打入发布物。
