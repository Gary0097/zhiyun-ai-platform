# 2.2.1 候选安装包验收

关联 #138；用户于 2026-09-14 要求生成安装包并试装。最终候选来源为 `e72a43cbd05d3526538748ce0795a42b0327e5d2`（基于已合并 master c1506fbb，增加本轮试装修复，修复待人工审核合并），运行时精确锁定 2.2.1。本记录不代表正式发布或所有业务能力验收通过。

## 产物

| 文件 | SHA256 |
|---|---|
| zhiyun-ai-os-v2.2.1-setup.exe | 0f65cdb96c5532e5531d67cc486aa5bf657a289c698db3da7d0ec6b60ed721bb |
| zhiyun-ai-os-v2.2.1-offline-usb.zip | 3ecc11b92a98183740c16be2d0c77e6512ba787914c3a76c5d72add1e8fd5466 |
| zhiyun-ai-os-v2.2.1-online-installer.zip | 8b23e44abb061810abdff1ace38530c97baffda6216cfe4bc4fb9006c4907063 |

Windows EXE 约 389 MiB；同源在线 ZIP 约 10.6 MiB。均为本地候选文件，未上传 Release、未生成生产签名清单或发布 Latest。包内包含更新入口及已审核的公钥；生产在线升级仍需签名发布及真实跨版本验收。

## 本轮实测

- 在独立工作树新建 2.2.1 单机及 Hub 环境，准备干净的 Windows 离线依赖缓存，不复用客户数据。
- `node apps/zhizaoyunAIOS/scripts/patch-console-ui.mjs` 及 `node scripts/verify-release.mjs` 通过。本轮完整运行时已存在，真实控制台品牌检查没有跳过。
- `node scripts/make-release-package.mjs 2.2.1 --offline --ref c1506fbb231d0da22ae34a6bbfcf1242003f478e --no-prune`、`node scripts/make-exe-installer.mjs 2.2.1` 及同提交在线 ZIP 打包成功。
- 两个 ZIP 的 CRC、精确运行时锁、来源提交、更新入口与公钥存在性检查通过。仅含工作区 `.gitignore` 占位，不含账号或用户内容。
- 在独立中文空格目录运行完整 EXE 的 `--dir` 安装，退出码 0；8088 `/api/version` 返回 2.2.1。
- 隔离实例原生认证：首次注册、登录、token 校验、匿名请求拒绝、拒绝第二个单机账号均通过。临时凭据只保存于忽略目录，不纳入仓库或包内。

## 覆盖安装发现与修复

首次候选（c1506fbb）首次安装通过，但运行中覆盖安装失败：安装器只结束托盘进程本身，遗留 cmd 子进程在运行时退出后暂停，并持有 launcher-service.log 写句柄，新服务无法重定向日志。该候选已保留到 failed-c1506fbb，不作为交付物。

安装器现对路径匹配的本安装启动器调用整棵进程树终止。新增真实回归：临时启动器拉起持有日志的子进程，验证升级停止后可以独占打开日志。原沙箱进程管理受限时失败；允许读取和结束测试进程的环境完整门禁通过，包含新增三项检查。

从 e72a43cbd0 重建最终候选，使用相同版本锁的原离线缓存归档，逐成员 CRC 校验后重用 52,516 个缓存文件。最终 EXE 对正在运行的隔离 2.2.1 实例覆盖安装退出 0，服务自动重启，版本返回 2.2.1；账号文件、工作区标记、Hub 配置和未知根目录文件摘要均不变，已有账号登录及匿名拦截再次通过。

这次是 2.2.1 同版本覆盖，不是 2.2.0 → 2.2.1 跨版本数据迁移验证。自动生成的临时测试账号在停服且确认摘要未变后移至忽略目录备份，验收版重新启动供用户自行注册。原 1.5.0 快捷方式和卸载登记已恢复，原目录未修改。

最终产物位于本机 H:/zhiyunAIOS/dist，试装实例为其中独立的中文空格目录。双平台源码门禁：https://github.com/Gary0097/zhiyun-ai-platform/actions/runs/34861725801 。

## 验收边界

本轮在现有 Windows 主机的独立目录试装，不等于另一台纯净机器或严格断网验收。安装运行时使用离线模式。Linux 包结构已核验，未做 Linux 实机安装；Hub 依赖就绪不等于 Hub 多租户、模型调用及集中凭据供给通过。没有执行完整卸载或生产签名跨版本升级，不能据此声称全部发布条件满足。

安装器仍使用单一产品的桌面及卸载注册项；试装前备份原 1.5.0 系统入口，原安装目录不修改。试装结束恢复原入口，验收版通过自身目录入口使用。

## 回退

候选不覆盖原安装目录。若验收实例不符合要求，停止其自身进程并保留目录以便诊断；不要使用会影响原安装登记的卸载入口。正式设备更新前应完整备份匹配版本的程序、工作区与凭据，保存新增数据后再按发行说明恢复，不能仅替换数据库。
